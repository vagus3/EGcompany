apiVersion: v1
kind: Service
metadata:
  name: escape-web
  namespace: escape-web
spec:
  selector:
    app: escape-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
